Artwork, motion clips and voice-over for Team Union (video cut) are published in here.

The filenames are the ones `scenario.yaml` declares — the board writes them, and the
projector opens exactly those names. Nothing here is edited by hand: see
docs/asset-pipeline.md.
